import React from 'react';

const ListView: React.FC = () => {
  return (
    <div className="content-area">
      {/*
        TODO 아래 컴포넌트 추가
          <SortButton />
          <KeywordItems />
          */}
    </div>
  );
};

export default ListView;
